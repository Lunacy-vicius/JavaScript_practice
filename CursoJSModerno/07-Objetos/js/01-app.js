//los objetos son la pieza principal en javascipt ya que pueden inicializarse en conjunto en vez de inicializar varias variables

const nombre ="Monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

//un objeto agrupa todo en una sola variable
//esto de abajo es un object literal
const producto = {
    nombre:"monitor de 20 pulgadas", 
    precio: 300,
    disponible: true
}

console.log(producto);