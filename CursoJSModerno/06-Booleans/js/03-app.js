
const autenticado = true;

//No es necesario ser redundante poniendo esto por ejemplo if(autentucado === true)
if(autentucado) {
    console.log("Si puedes entrar a tu cuenta")
}  else {
        console.log("No no puedes verlo")
}

//Operador ternario 
console.log(autenticado ? "si esta autenticado" : "No esta autenticado");
