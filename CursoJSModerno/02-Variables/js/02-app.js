// variables con let
// Una variable no puede tener el mismo nombre, no es lo mismo que reasignar su valor, aunque se encuentre en archivos distintos, si estos estan ligados al mismo HTML habra un error en consola.
let producto = "Tablet";
// reasignar valor
producto = "Monitor";

producto = 20;
producto = true;
producto = null;

console.log(producto);

let precio;

precio = 300;

console.log (precio);